import type {CapacitorConfig} from '@capacitor/cli';
const config:CapacitorConfig={appId:'de.aplussolution.workforce',appName:'A+ Solution',webDir:'dist',server:{androidScheme:'https',iosScheme:'https'},plugins:{StatusBar:{style:'DARK'},Keyboard:{resize:'body'}}};
export default config;
