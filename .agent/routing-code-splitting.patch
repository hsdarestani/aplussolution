diff --git a/frontend/src/App.tsx b/frontend/src/App.tsx
index e398f86..0000000 100644
--- a/frontend/src/App.tsx
+++ b/frontend/src/App.tsx
@@ -1,4 +1,4 @@
-import React, { useEffect, useMemo, useState } from 'react';
+import React, { lazy, Suspense, useEffect, useMemo, useState } from 'react';
 import {
   IonApp,
   IonBadge,
@@ -42,16 +42,18 @@ import {
   trashOutline,
 } from 'ionicons/icons';
 import { api, consumeOAuth, login, logout, me, socialUrl, User } from './api';
-import Operations from './Operations';
-import ScheduleV2 from './ScheduleV2';
-import AttendanceV3 from './AttendanceV3';
 import ActivationPage from './ActivationPage';
-import EmployeeHome from './EmployeeHome';
-import PortalAccessPanel from './PortalAccessPanel';
-import AdminHomeV4 from './AdminHomeV4';
-import GlobalSearch from './GlobalSearch';
 import ListToolbar from './ListToolbar';
-import DocumentCenterV5 from './DocumentCenterV5';
+
+const Operations = lazy(() => import('./Operations'));
+const ScheduleV2 = lazy(() => import('./ScheduleV2'));
+const AttendanceV3 = lazy(() => import('./AttendanceV3'));
+const EmployeeHome = lazy(() => import('./EmployeeHome'));
+const PortalAccessPanel = lazy(() => import('./PortalAccessPanel'));
+const AdminHomeV4 = lazy(() => import('./AdminHomeV4'));
+const GlobalSearch = lazy(() => import('./GlobalSearch'));
+const DocumentCenterV5 = lazy(() => import('./DocumentCenterV5'));
@@ -127,6 +129,24 @@ const nav: Record<string, [View, string][]> = {
   ],
 };
 
+const viewFromLocation = (role: string): View => {
+  if (window.location.pathname !== '/') return 'dashboard';
+  const requested = new URLSearchParams(window.location.search).get('view');
+  if (!requested) return 'dashboard';
+  if (requested === 'profile') return 'profile';
+  return (nav[role] || nav.worker).some(([key]) => key === requested)
+    ? (requested as View)
+    : 'dashboard';
+};
+
+const urlForView = (view: View) => {
+  const url = new URL(window.location.href);
+  if (view === 'dashboard') url.searchParams.delete('view');
+  else url.searchParams.set('view', view);
+  return `${url.pathname}${url.search}${url.hash}`;
+};
+
 const unpack = (data: any): any[] => data?.results || data || [];
 const value = (event: any) => event.detail.value ?? '';
 const isManager = (user: User) => ['admin', 'manager'].includes(user.role);
@@ -3034,6 +3054,24 @@ export default function App() {
     return () => window.removeEventListener('auth-lost', lost);
   }, []);
 
+  useEffect(() => {
+    if (!user || window.location.pathname !== '/') return;
+    const syncFromLocation = () => {
+      const next = viewFromLocation(user.role);
+      const requested = new URLSearchParams(window.location.search).get('view');
+      setView(next);
+      setMobileMenuOpen(false);
+      if (requested && (requested !== next || next === 'dashboard')) {
+        window.history.replaceState({ view: next }, '', urlForView(next));
+      }
+      window.requestAnimationFrame(() => window.scrollTo({ top: 0 }));
+    };
+    syncFromLocation();
+    window.addEventListener('popstate', syncFromLocation);
+    return () => window.removeEventListener('popstate', syncFromLocation);
+  }, [user]);
+
   if (location.pathname === '/aktivieren') return <IonApp><ActivationPage /></IonApp>;
   if (location.pathname === '/datenschutz') return <Legal />;
   if (location.pathname === '/konto-loeschen') return <Legal deletePage />;
@@ -3075,8 +3113,12 @@ export default function App() {
     messages: 'Chat',
   };
   const navigateTo = (next: View) => {
-    setView(next);
+    const safe = next === 'profile' || items.some(([key]) => key === next) ? next : 'dashboard';
+    if (safe !== view) {
+      window.history.pushState({ view: safe }, '', urlForView(safe));
+    }
+    setView(safe);
     setMobileMenuOpen(false);
     window.requestAnimationFrame(() => window.scrollTo({ top: 0, behavior: 'smooth' }));
   };
@@ -3148,7 +3190,12 @@ export default function App() {
               </IonButton>
             </aside>
 
-            <main className="app-main">{isManager(user) && <GlobalSearch onNavigate={navigateTo} />}{content}</main>
+            <main className="app-main">
+              <Suspense fallback={<Loader />}>
+                {isManager(user) && <GlobalSearch onNavigate={navigateTo} />}
+                {content}
+              </Suspense>
+            </main>
           </div>
         </IonContent>
