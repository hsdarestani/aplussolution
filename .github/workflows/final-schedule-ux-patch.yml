name: Final schedule UX patch

on:
  push:
    branches:
      - feature/final-notification-copy-splash
    paths:
      - scripts/final_schedule_ux_followup.py
      - .github/workflows/final-schedule-ux-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/final-notification-copy-splash
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply final follow-up
        run: python scripts/final_schedule_ux_followup.py
      - name: Verify patch
        run: |
          git diff --check
          python -m compileall backend/core backend/tests
      - name: Commit source and regression tests
        run: |
          if git diff --quiet; then
            echo 'No source changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add backend frontend
          git commit -m 'Harden reassignment and align final Dienstplan QA'
          git push origin HEAD:feature/final-notification-copy-splash
